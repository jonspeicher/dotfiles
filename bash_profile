# .bash_profile is the user-specific system-wide configuration file for the Bourne Again Shell
# (bash). .bash_profile is used for login shells (e.g. remote ssh), while .bashrc is used for non-
# login interactive shells (e.g. Terminal).

# Source .bashrc if it exists.

if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi
