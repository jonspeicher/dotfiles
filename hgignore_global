# .hgignore_global is referred to by .hgrc to configure a system-wide ignore
# pattern.

syntax: glob

# On a Mac, ignore .DS_Store.

.DS_Store

# Don't show Git repos where they appear in the same place as Mercurial repos.

.git