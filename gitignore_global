# TBD: Clean up naming, comments, git-scm.com reference
#
# .gitignore_global is referred to by .gitconfig to configure a system-wide
# ignore pattern.

# ~/.gitignore_global is the user-specific path attributes configuration file for git. To work, this
# file must be specified as the global configuration file using core.attributesfile. See
# http://git-scm.com/docs/gitattributes.html for more information.

# Always ignore vim swap files.

*.swp

# On a Mac, ignore .DS_Store.

.DS_Store

# Don't show Mercurial repos where they appear in the same place as Git repos.

.hg
